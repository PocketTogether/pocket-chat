当然可以，我给你画一张 **完全基于你当前 PocketBase Presence 系统设计** 的 **Mermaid 架构图**。  
这张图可以直接放进：

- VSCode 的 Markdown
- Obsidian
- GitHub README
- JSDoc（作为代码块）

并且会被自动渲染成图。

下面就是你要的 **Mermaid 图**。

---

# 🟦 Presence 系统完整 Mermaid 架构图（可直接复制）

```mermaid
flowchart TD

%% ========= 前端部分 =========
subgraph FE["Frontend (Vue)"]
  L1["5 秒循环：读取 store 状态（是否输入、是否可见）"]
  L2["比较上次发送的数据"]
  L3{"状态是否变化？"}
  L4{"距离上次发送完成是否 < 2 秒？"}
  L5["发送 userPresencesStatus 到后端"]

  B1["visibilitychange hidden 触发"]
  B2["navigator.sendBeacon 调用前判断是否有必要发送"]
  B3["创建 userNotViewingMarks 到后端"]

  L1 --> L2 --> L3
  L3 -->|否| L1
  L3 -->|是| L4
  L4 -->|是| L1
  L4 -->|否| L5

  B1 --> B2 --> B3
end

%% ========= PocketBase 集合 =========
subgraph PB["PocketBase Backend"]
  P1["userPresencesStatus 集合\n- user\n- isScreenVisible\n- isTyping\n- created"]
  P2["userNotViewingMarks 集合\n- user\n- created"]

  M1["获取该用户最新 userPresencesStatus 记录"]
  M2["获取该用户最新 userNotViewingMarks 记录"]
  M3{"哪个记录时间更新更晚？"}
  M4["若 mark 更新更晚：认为 isScreenVisible = false"]
  M5["否则：使用 presence 中的 isScreenVisible"]
  M6{"presence.created 是否在 100 秒内？"}
  M7["判定为 在线"]
  M8["判定为 离线"]

  P1 --> M1
  P2 --> M2
  M1 --> M3
  M2 --> M3
  M3 -->|mark 更新更晚| M4 --> M6
  M3 -->|presence 更新更晚| M5 --> M6
  M6 -->|是| M7
  M6 -->|否| M8
end

%% ========= 数据流连接 =========
L5 --> P1
B3 --> P2

```


---

# 🟦 这张图表达了什么？

它完整表达了你系统的三大核心机制：

## **1. 前端状态循环（5 秒）**

- 检查输入状态
- 检查可见性
- 比较上次发送的数据
- 节流（2 秒内不重复发送）
- 状态变化立即发送

## **2. sendBeacon（visibilitychange hidden）**

- 页面隐藏时立即发送
- 不带鉴权
- 创建 userNotViewingMarks
- 避免重复发送

## **3. PocketBase 后端**

- `userPresencesStatus`：主状态快照
- `userNotViewingMarks`：补充“未查看”标记
- 合并逻辑：
    - 哪个记录更新更晚
    - 判断屏幕是否显示
    - 判断是否在线（100 秒规则）

---

# 🟦 如果你愿意，我还能继续为你生成：

### ✔ PlantUML 版本（可导出 ASCII 图）

### ✔ ASCIIFlow 版本（纯字符 UI 布局图）

### ✔ Presence 系统的 UI 布局图（Mermaid / PlantUML）

### ✔ Presence 系统的前端状态机图（Mermaid stateDiagram）

### ✔ Presence 系统的 ER 图（Mermaid erDiagram）

你想继续哪一种？