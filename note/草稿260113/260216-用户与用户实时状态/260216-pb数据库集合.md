### messages 消息集合的一些修改
```
mentionedUsers 新增关系字段，关系至users集合，是可多的，用于记录本消息所@的用户
mentionMap 新增json字段，用于记录消息中用户名所对应的用户id，键值对，需要这个主要是用来明确指定消息内容中 @xxxx（username）所指定的用户，因为username是可能被用户修改的
示例
{
  "haruki": "abc123",
  "alice": "xyz789"
}

export const mentionMapSchema = z.record(
  z.string(), // username
  z.string()  // userId
);

api规则不用改
```

### usersPresencesStatus 用户实时状态集合
```
usersPresencesStatus

字段
author 用户关系字段
isNotViewing 是否不在看（是否屏幕正在不显示）、布尔
isTyping 是否正在输入、布尔

```

查看规则
```c
// 访问主体的前置条件 ，根据配置值判断未登录用户是否可以查看
(
  // 用户已登录，可以查看消息
  @request.auth.id != '' ||
  // 根据config，判断是否允许未登录用户查看消息
  (
    @collection.config:allowAnonymousView.key ?= 'allow-anonymous-view' &&
    @collection.config:allowAnonymousView.value ?= true
  )
) &&
// 访问主体的权限条件，只有isBanned为false的用户才能访问
(
  // 用户未登录时可通过，因为匿名访问已在前置条件控制
  @request.auth.id = '' ||
  // 用户已登录，检查其isBanned 为 false 才能通过
  (
    @collection.users:isBannedCheck.id ?= @request.auth.id &&
    @collection.users:isBannedCheck.isBanned ?= false
  )
)
```

创建规则
```c
// 访问主体的前置条件，需登录
@request.auth.id != '' &&
// 访问主体的权限条件，非isBanned
(
  @collection.users:isBannedCheck.id ?= @request.auth.id &&
  @collection.users:isBannedCheck.isBanned ?= false
) &&
// 资源本身的可访问性条件，创建时需为创建者（传入的author需为当前用户）
@request.body.author = @request.auth.id
```

不能修改与删除

### usersNotViewingMarks 用户未查看标记集合
```
usersNotViewingMarks

字段
author 用户关系字段
```

查看规则
```c
// 访问主体的前置条件 ，根据配置值判断未登录用户是否可以查看
(
  // 用户已登录，可以查看消息
  @request.auth.id != '' ||
  // 根据config，判断是否允许未登录用户查看消息
  (
    @collection.config:allowAnonymousView.key ?= 'allow-anonymous-view' &&
    @collection.config:allowAnonymousView.value ?= true
  )
) &&
// 访问主体的权限条件，只有isBanned为false的用户才能访问
(
  // 用户未登录时可通过，因为匿名访问已在前置条件控制
  @request.auth.id = '' ||
  // 用户已登录，检查其isBanned 为 false 才能通过
  (
    @collection.users:isBannedCheck.id ?= @request.auth.id &&
    @collection.users:isBannedCheck.isBanned ?= false
  )
)
```


允许创建

不能修改与删除




## TODO
```
pb用户实时状态相关的集合的规则更加严谨，都加上游客是否能查看的判断
usersNotViewingMarks 查看时需登录，创建时无需登录

索引pb所有集合中字段按需尽量加索引
```