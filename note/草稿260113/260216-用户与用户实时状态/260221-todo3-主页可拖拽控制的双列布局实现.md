```
帮我实现
- 可拖拽控制宽度
- 可收起或展开

注意是有限制的
- 左栏不小于375，右栏不小于400
- 而且不仅应在拖拽时限制，显示时也应通过计算属性

要做好日后将宽度或是否展开记忆封装至store持久化的准备，暂不必实现


上边的文字是我的提示词，帮我写更好更精确的提示词，顺便给我讲讲怎样实现
```


```
请为以下布局实现左栏宽度控制与展开/收起功能：

### 1. 功能需求

- 左栏宽度可通过拖拽进行调整（拖拽线位于左栏右侧）。
    
- 左栏可被收起，也可再次展开。
    
- 拖拽时实时更新宽度。
    
- 展开/收起按钮应正常工作。
    

### 2. 限制条件

- 左栏宽度不得小于 **375px**。
    
- 右栏宽度不得小于 **400px**。
    
- 限制不仅在拖拽时生效，**显示时也必须通过计算属性强制限制**。
    

### 3. 状态管理要求

- 需要为未来将宽度与展开状态持久化到 store 做好准备（例如 pinia）。
    
- 因此宽度与展开状态应使用 `ref` 或 `reactive` 管理，并通过计算属性暴露最终宽度。
    

### 4. 技术要求

- 使用 Vue 3 `<script setup>` + TypeScript。
    
- 使用 pointer 事件实现拖拽（pointerdown / pointermove / pointerup）。
    
- 代码结构需清晰、可扩展、可维护。

```


```
TODO
// 新增一个5档可调节的，主要用于，控制最大宽度，也像别的值一样右set之类的便于未来弄store
// 2000 1600 1400 1200(默认) 1000


```

[260226-问题记录flex-1的元素中overflow-hidden导致sticky失效但不加又会导致内容不稳定有时会超级宽，解决办法是min-w-0](260226-问题记录flex-1的元素中overflow-hidden导致sticky失效但不加又会导致内容不稳定有时会超级宽，解决办法是min-w-0.md)
```
转到正式使用时，发现

<!-- 右栏 -->
<div class="flex-1 overflow-hidden">
会破坏其中内容的 sticky top-0

而不加 overflow-hidden 又会导致内容不稳定，很奇怪的是一开始正常，向上滚动加载后内容就会变得非常宽
解决办法是 min-w-0
```

```
TODO
不实时拖动，而是放开后再应用
```
