准备所需的api与query
```
文件 src\api\users-status.ts

pbUsersPresencesStatusSubscribeAllApi
userPresencesStatus实时订阅api函数，参数是回调函数

pbUsersPresencesStatusInitGetListApi
userPresencesStatus查询前500条函数

pbUsersNotViewingMarksSubscribeAllApi
usersNotViewingMarks实时订阅api函数，参数是回调函数

pbUsersPresencesStatusCreateApi 函数
usersPresencesStatus集合的创建记录接口

pbUsersNotViewingMarksCreateApiBySendBeacon 函数
usersNotViewingMarks集合的创建记录接口，
不过用的不再是pb sdk了，用的是 navigator.sendBeacon



文件 src\queries\users-status.ts

useUsersPresencesStatusInitGetListQuery
queryKey: ['usersPresencesStatusInitGetList']
会使用 pbUsersPresencesStatusInitGetListApi
```

store这一块
```
useRealtimeUsersPresencesStatusStore
文件 src\stores\realtime-users-presences-status.ts

useRealtimeUsersNotViewingMarksStore
文件 src\stores\realtime-users-not-viewing-marks.ts

useSelfPresenceStore
文件 src/stores/self-presence.ts
```

全部用户的实时状态订阅接收与处理 这一块
```
useRealtimeUsersStatusSubscription
文件 src\composables\realtime-users-status\subscription.ts

useRealtimeUsersStatusComputed
文件 src\composables\realtime-users-status\computed.ts
```

用户自己的实时状态循环检查与发送 这一块
```
useSelfPresenceDispatcher
文件 src/composables/self-presence/dispatcher.ts
用于封装判断与发送逻辑，提供方法供外界调用，
如：判断是否需要发送、执行发送、立即触发一次判断与发送等。

useSelfPresenceLoopAndViewingHook
文件 src/composables/self-presence/loop-hook.ts
用于在 app.vue 中，调用本组合式函数中的某个函数，启动无限循环与监听 visibilitychange，
内部会配合 store 中的响应式布尔值标记避免重复启动，
会使用 useSelfPresenceDispatcher ，循环中会调用SelfPresenceDispatcher中的方法。

useSelfPresenceTypingReporter
文件 src/composables/self-presence/typing-reporter.ts
用于供输入栏组件调用，更新 store 中的输入状态，
还能在在第一次输入或发送后调用SelfPresenceDispatcher中的方法，以便立即判断并决定是否立即发送。
```

组合式函数的实际使用
```
src\App.vue 中，启动用户实时状态订阅，启动当前状态循环检查

输入栏使用 useSelfPresenceTypingReporter
@input 与 消息发送后 调用

测试
```

完善 useRealtimeUsersStatusComputedForUserRealtimeStatus 等方面
```
userRealtimeStatusForShow 计算属性，使用userRealtimeStatusWithIsOnline的数据，将其处理为适合使用的数据如
{ key、颜色、文字 }
离线 var(--color-text-soft)
在线 var(--poto-color-moss-green)
输入中 var(--poto-color-mist-blue)
闲置 var(--poto-color-amber-sand)

文字是要使用i18nStore
src\config\i18n\messages\users-status.ts
```

优化数据处理性能，将数据处理放在queryFn
```
[260307-1649-数据处理放在queryFn中更好，而不是组合式函数](../../../笔记251120/260307-1649-数据处理放在queryFn中更好，而不是组合式函数.md)
```

实现用户状态的显示
```
一个新的vue组件，其能借助key来决定图标
src\components\user\UserRealtimeStatusToIcon.vue

在用户列表 用户项 中使用

在用户详情中使用

在聊天列中 消息项 中使用
```

上次在线时间实现
```
新增api
pbUsersPresencesStatusGetFirstListItemByUserIdApi

新增query
useUsersPresencesStatusGetFirstListItemByUserIdQuery

在用户详情中使用
```

动态页实现
```
路由
```

changelog、readme
```
目前这次更新新增了这些功能
- 支持消息搜索，并可在消息中通过 “#标签” 来快速跳转至搜索页
- 支持用户艾特功能，可在消息中使用 “@username”，点击后可查看用户详情
- 支持用户实时状态，如 “在线”、“离线”、“闲置”、“输入中”

我想编一些对话，以便我截图后当作轻量的新功能介绍，同时最主要的目的是当作本项目的预览
像这样的，类似 A开发者 B用户 这样的对话，逗哏捧哏这一块

A: PocketChat支持xxxxx了……
B: ……
A: ……
B: ……
10句左右，先给我中文的
并不一定要a一句b一句，ab时不时连发两句三句更好
```

```
**A（开发者）**：我跟你说，这次 PocketChat 更新真的狠。 
**A**：你现在点消息里的 **#标签**，能直接跳到搜索页，像瞬移一样。

**B（用户）**：哇，那我以后找消息不用像考古一样挖半天了。 
**B**：不过我聊天是不是得开始自觉打标签了，比如 #我又迟到了。

**A**：随便你，但你要是被同事点到 #摸鱼，那我可不负责。

**A**：还有，消息里可以直接 **@用户名** 艾特别人，点一下还能看用户详情。 

**A**：更刺激的是，现在还能看到实时状态：在线、离线、闲置、输入中，全都一目了然。 
**B**：那我看到“输入中”三个字，是不是要开始紧张。 
**B**：尤其是如果那个人是我老板。

**A**：那你不仅要紧张，还要赶紧回一句“马上处理”。
```

```
**A (Developer):** 
I’m telling you, this PocketChat update is wild.  
**A:** 
Now you can tap any #tag in a message and jump straight to the search page—like teleporting.

**B (User):** 
Wow, so I don’t have to dig through messages like an archaeologist anymore.  
**B:** 
But does this mean I should start tagging myself… like #LateAgain ?

**A:** 
Do whatever you want, but if your coworkers tap on #Slacking, that’s on you.
**A:** 
Oh, and now you can @soyorin (@username) to mention someone, and tapping it shows their profile.  
**A:** 
And here’s the spicy part: real‑time status. Online, Offline, Idle, Typing… all visible. 
 
**B:** 
So every time I see “Typing…”, I should start panicking.  
**B:** 
Especially if the person typing is my boss.

**A:** 
In that case, don’t just panic—reply with “On it right away.”
```
