关于pocket-chat，有这些页面\路由
```
主页（聊天页）
普通的单列布局，显示聊天内容，有顶栏底栏，底栏即聊天输入栏，
底栏在未输入时还会显示一个按钮，点击其会展开一个菜单，可以通过此菜单跳转到 图片选择页、文件选择页
当未登录时，底栏会显示登录按钮，点击即跳转至登录页
（其实 E:\Project\pocket-chat\vue3\src\views\chat-home\ChatHome.vue 中的内容很少，
主要内容都在E:\Project\pocket-chat\vue3\src\components\chat\chat-col 这一块里）

设置页
登录页
图片选择页
图片详情页
文件选择页
文件详情页
```
因为pocket-chat正在开发中，后续会有新的路由，可能未写在此处，
不过无所谓，不影响此提示词的使用

这是 router.ts ，我挑重点简单写一下
`E:\Project\pocket-chat\vue3\src\router.ts`
```ts
import {
  ChatHome,
  ChatSetting,
  FileInfoPage,
  FileSelectPage,
  ImageInfoPage,
  ImageSelectPage,
  LayoutSimple,
  LoginPage,
} from './views'
import { routerDict } from './config'
// ...
const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      ...routerDict.LayoutSimple,
      component: LayoutSimple,
      redirect: routerDict.ChatHome.path,
      children: [
        {
          ...routerDict.LoginPage,
          component: LoginPage,
        },
        {
          ...routerDict.ChatHome,
          component: ChatHome,
        },
        {
          ...routerDict.ChatSetting,
          component: ChatSetting,
        },
        {
          ...routerDict.ImageSelectPage,
          component: ImageSelectPage,
        },
        {
          ...routerDict.ImageInfoPage,
          component: ImageInfoPage,
        },
        {
          ...routerDict.FileSelectPage,
          component: FileSelectPage,
        },
        {
          ...routerDict.FileInfoPage,
          component: FileInfoPage,
        },
        // ...
      ],
    },
  ],
})
// ...
```

这是`E:\Project\pocket-chat\vue3\src\views\layout\LayoutSimple.vue`
```vue
<script setup lang="ts"></script>
<template>
  <div class="layout-simple">
    <RouterView></RouterView>
  </div>
</template>
<style lang="scss" scoped>
.layout-simple {
  max-width: 2000px;
  margin: 0 auto;
}
</style>
```

这是（挑视图重点简单写了点）`E:\Project\pocket-chat\vue3\src\App.vue`
```vue
<template>
  <div>
    <!-- ... -->
          <!-- 主滚动条 -->
          <ElScrollbar
            ref="appMainElScrollbar"
            height="100dvh"
            class="appMainElScrollbar"
          >
            <RouterView></RouterView>
          </ElScrollbar>
  </div>
</template>
```

这是`E:\Project\pocket-chat\vue3\src\config\router.ts`
```ts
interface RouterItem {
  path: string
  name: string
  paramsKey?: Record<string, string>
}

// 路由信息字典，导出以便整个程序中使用
export const routerDict = {
  ChatHome: {
    path: '/',
    // name: 'HomePage',
    name: 'ChatHome',
  },
  ChatSetting: {
    path: '/setting',
    name: 'SettingPage',
  },
  LayoutSimple: {
    path: '/LayoutSimple',
    name: 'LayoutSimple',
  },
  LoginPage: {
    path: '/login',
    name: 'LoginPage',
  },
  ImageSelectPage: {
    path: '/image-select',
    name: 'ImageSelectPage',
  },
  ImageInfoPage: (() => {
    const paramsKey = {
      id: 'id',
    } as const
    return {
      // 使用paramsKey拼接参数字符串，安心安全安定
      path: `/image-info/:${paramsKey.id}`,
      name: 'ImageInfoPage',
      // 导出paramsKey以便页面中使用
      paramsKey,
    } as const
  })(),
  FileSelectPage: {
    path: '/file-select',
    name: 'FileSelectPage',
  },
  FileInfoPage: (() => {
    const paramsKey = {
      id: 'id',
    } as const
    return {
      // 使用paramsKey拼接参数字符串，安心安全安定
      path: `/file-info/:${paramsKey.id}`,
      name: 'FileInfoPage',
      // 导出paramsKey以便页面中使用
      paramsKey,
    } as const
  })(),
} as const satisfies Record<string, RouterItem>
```
